import os
from argparse import ArgumentParser
from typing import Iterator

from utils import logger, parse_input


class {{class}}:

    def __init__(self, {{doc_name}}: Iterator[str], part: int = 1):
        self.part = part

        self.{{element}}s = [line for line in self.parse_lines({{doc_name}})]

    def __str__(self) -> str:
        return f"{self.__class__.__name__}(part {self.part})"

    def parse_line(self, y: int, line: str) -> str:
        return line

    def parse_lines(self, lines: Iterator[str]) -> Iterator:
        for y, line in enumerate(lines):
            yield self.parse_line(y, line)

    def esrap_line(self, y: int, element: str) -> str:
        return f"{element}"

    def esrap_lines(self) -> str:
        return '\n'.join(
            self.esrap_line(y, e) for y, e in enumerate(self.{{element}}s)
        )


def {{p2_function}}({{obj}}: {{class}}, **_: dict) -> int:
    accumulator = 0

    # solve part 2

    return accumulator


def {{p1_function}}({{obj}}: {{class}}, **_: dict) -> int:
    accumulator = 0

    # solve part 1

    return accumulator


INPUT_PARAMS = {
    ('test.txt'): {
    },
    ('input.txt'): {
    }
}


arg_parser = ArgumentParser('python -m {{year}}.{{day}}.main', description="Advent of Code {{ year }} Day {{ day }}")
arg_parser.add_argument('input_path', help="Path to the input file")
arg_parser.add_argument('part', type=int, default=1, help="Which part of the challenge to apply.")

if __name__ == '__main__':
    argus = arg_parser.parse_args()

    params = INPUT_PARAMS[os.path.basename(argus.input_path)]
    {{doc_name}} = parse_input(argus.input_path)
    {{obj}} = {{class}}({{doc_name}}, part=argus.part)
    match argus.part:
        case -1:
            answer = {{obj}}.esrap_lines()
        case 1:
            answer = {{p1_function}}({{obj}}, **params)
        case 2:
            answer = {{p2_function}}({{obj}}, **params)

    logger.debug('')

    if answer:
        print(f"{answer}")
    else:
        print(f"No answer available for part {argus.part}")
